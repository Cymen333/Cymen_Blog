// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 把你的 GitHub 用户名替换到下面
  site: 'https://github.com/Cymen333/Cymen_Blog.git', 
  // 你的 GitHub 仓库名字（必须加斜杠）
  base: '/my-blog-Astro', 
});
